# Ejemplo de config/secrets.production.enc (SOPS + age)
# Generar con: sops --encrypt config/.env.example > config/secrets.production.enc
# SOPS_AGE_KEY del dev debe descifrar el recipient de .sops.yaml

DB_HOST=10.43.x.x
DB_PORT=3306
DB_USER=chat_user
DB_PASSWORD=secret
DB_NAME=chat_db
RABBITMQ_URL=amqp://guest:guest@10.43.x.x:5672